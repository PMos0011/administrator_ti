#ifndef FUNCTIONS_H
#define FUNCTIONS_H
#include <iostream>

bool Unzip_file(std::string name, std::string &short_name);
bool Crate_structure(std::string short_name, std::string name);

#endif // FUNCTIONS_H
